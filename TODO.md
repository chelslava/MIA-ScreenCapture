# TODO: Краткосрочные Задачи До `v1.4.5`

> Обновлено: 2026-03-28
> Статус: активный список до ближайшего релиза

## P0 (обязательно до релиза)

- [ ] Выполнить ручной 30+ минутный GUI smoke-run API-вкладки с открытым
  окном логов и зафиксировать результат в release-note.
- [ ] Проверить смену дня для live-логов API в реальном времени
  (полуночная ротация `api_YYYY-MM-DD.log` без потери хвоста).
- [ ] Убрать синхронный `stop` через GUI-поток из API пути:
  реализовать асинхронную остановку записи (`202 Accepted` + статус операции),
  чтобы исключить `TimeoutError` и `500` на длительной финализации.
- [ ] Добавить детальную диагностику FFmpeg при live-записи:
  сохранять `stderr` в кольцевой буфер/файл и выводить хвост в лог при
  ошибках финализации (`moov atom not found`, broken pipe, timeout).
- [ ] Сделать корректный graceful-stop API сервера при `restart`:
  дождаться фактического освобождения порта перед повторным стартом.

## P1 (важно после P0)

- [ ] Убрать `capture_output=True` для долгих `ffmpeg` процессов в
  `recorder/encoder.py`, перейти на потоковое чтение stderr, ограничить
  буфер и логировать прогресс без роста памяти.
- [ ] Снизить overhead API access-логов:
  перевести детализацию в `DEBUG` или добавить sampling/rate-limit для
  высокочастотных маршрутов (`status`, `events`, `health`).
- [ ] Добавить lock-защиту чтения `_tasks` в планировщике
  (`get_all_tasks`, `get_task_count`) для исключения race-condition.

## P2 (техдолг/оптимизация)

- [ ] Оптимизировать `ConfigManager.save()` для горячих сценариев:
  debounce/batch запись `config.json`, чтобы убрать частые синхронные I/O.
- [ ] Удалить или задействовать `self._frame_queue` в `VideoRecorder`:
  сейчас очередь не используется и усложняет поддержку.

## Правило ведения TODO

- Закрытые задачи удаляем из файла, не оставляем в виде `[x]`.
- Незакрытые задачи переносим в новый TODO ближайшего релиза.
