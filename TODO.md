# TODO: Краткосрочные Задачи До `v1.4.4`

> Обновлено: 2026-03-28
> Статус: активный список до ближайшего релиза

## P0 (обязательно до релиза)

- [ ] Проверить сценарии `Запустить`/`Остановить`/`Перезапустить` API из GUI
  на реальном окружении (3 последовательных цикла).
- [ ] Проверить live-логи API на ротации по дате и при пустом лог-файле.
- [ ] Убедиться, что изменение порта/токена из вкладки API корректно
  сохраняется и применяется после перезапуска сервера.

## P1 (желательно до релиза)

- [ ] Добавить unit-тесты для runtime управления API из `main.py`.
- [ ] Добавить unit-тесты для `gui/views/api_settings_view.py`.
- [ ] Вычистить mypy-ошибки в затронутых API/GUI модулях.
- [ ] Уточнить тексты ошибок в API-вкладке (более понятные пользователю).

## P2 (можно перенести)

- [ ] Улучшить UX списка последних записей (фильтрация/быстрые действия).
- [ ] Подготовить технический дизайн WebSocket transport для `v1.5.0`.

## Заметки к релизу

- Release candidate формировать после закрытия всех P0.
- При переносе задач в следующий релиз фиксировать причину переноса.
